---
name: slide-library
description: 슬라이드 라이브러리 스킬. 두 가지 일을 한다. (1) 선택·제작 — 원본 파일 없이 발표 주제·상황·무드를 자연어로 말하면, 35종 HTML 슬라이드 템플릿 중 어울리는 것을 골라 실제 덱까지 만들어 준다. (2) 추출 — PDF나 PowerPoint 덱을 첨부하면 그 덱의 색·타이포·레이아웃·카피 규칙을 §1~§10 시스템 프롬프트로 역추출한다. 사용자가 "슬라이드/발표자료/피치덱/IR/보고서 만들어줘", "이 주제에 어울리는 템플릿 골라줘", "이 상황에 맞는 덱 만들어줘", "35개 중에 골라줘" 같은 표현을 쓰거나, 슬라이드 파일(PDF·PPT)을 첨부하고 "이 디자인 추출/분석/복제해줘", "이 스타일로 프롬프트 만들어줘", "템플릿 추출해줘"라고 하면 반드시 발동한다. 슬라이드·덱·프레젠테이션을 새로 만들거나 기존 덱의 디자인을 재사용하려는 의도가 조금이라도 보이면, 사용자가 "slide-library"를 직접 부르지 않아도 이 스킬을 쓴다. 단순 PDF 텍스트 추출·요약, 슬라이드가 아닌 단일 그래픽 디자인, 슬라이드와 무관한 일반 문서 작성에는 쓰지 않는다.
compatibility: 추출 모드에 한해 PDF(네이티브 읽기 + 페이지 렌더링), PPTX(zip 해제로 테마·색·폰트 직접 조회). 색 샘플링에 Python Pillow, 폰트 조회에 pdffonts 권장(없어도 근사 가능). 선택 모드는 이 저장소의 docs/index.json과 docs/templates/를 사용한다.
---

## 이 스킬이 하는 일

슬라이드 라이브러리는 서로 이어지는 두 가지 능력을 가진다.

- **선택·제작 모드** — 만들 원본이 없을 때. 사용자가 "이런 발표를 해야 한다"고 말하면, 35종 템플릿 중 톤이 맞는 것을 골라 실제 슬라이드 덱까지 만들어 준다.
- **추출 모드** — 이미 잘 만든 덱이 있을 때. 그 PDF/PPT를 분석해 색·타이포·레이아웃·카피 규칙을 §1~§10 시스템 프롬프트로 역추출한다. 이후로는 그 프롬프트 한 줄로 같은 품질을 반복 생산할 수 있다.

두 모드는 반대 방향의 같은 목표를 향한다. **좋은 슬라이드를 반복 가능하게 만드는 것.** 선택 모드는 검증된 템플릿에서 출발하고, 추출 모드는 사용자가 아끼는 덱을 재사용 자산으로 바꾼다.

## 먼저: 어느 모드인가

작업을 시작하기 전에 딱 하나만 판단한다. **분석할 원본 슬라이드 파일이 있는가?**

- **PDF/PPTX가 첨부됐고**, 그 파일의 디자인을 분석·추출·복제하려는 의도다 → **추출 모드**. ("이 덱 분석해줘", "이 스타일 뽑아줘", "이걸로 프롬프트 만들어줘")
- **원본 파일이 없고**, 새 발표/문서를 위한 슬라이드를 원한다 → **선택·제작 모드**. ("이 주제로 덱 만들어줘", "어울리는 템플릿 골라줘", "IR 발표 슬라이드 필요해")
- **애매하면 한 줄로 확인한다.** 예: "가지고 계신 PDF/PPT의 디자인을 뽑아 드릴까요, 아니면 주제에 맞는 템플릿을 골라 새로 만들어 드릴까요?" 추측으로 엉뚱한 모드에 들어가 시간을 버리지 않는다.

첨부 파일이 있어도 그게 분석 대상이 아니라 단순 참고 자료(예: "이 보고서 내용으로 슬라이드 만들어줘")면 선택·제작 모드다. 판단 기준은 "그 파일의 **디자인을 복제**하려는가"이다.

---

## 선택·제작 모드

목표는 두 단계다. **(1) 톤이 맞는 템플릿을 고르고, (2) 그 템플릿으로 사용자의 실제 콘텐츠를 채운 덱을 만든다.** 사용자가 추천만 원하면 (1)에서 멈춰도 된다.

이 모드의 상세 워크플로우·매칭 기준·템플릿 확장 규칙·함정은 저장소 루트의 **`AGENTS.md`**에 완결돼 있다. 작업 전 `AGENTS.md`를 읽고 그 6단계를 따른다. 템플릿 카탈로그(35종의 무드·톤·용도 메타데이터)는 **`docs/index.json`**, 실제 템플릿 파일은 **`docs/templates/<slug>/`**에 있다.

핵심 흐름을 요약하면,

1. **먼저 상황과 무드를 묻는다.** 파일을 읽기 전에 "① 어떤 자리인가(피치, 리서치 공유, 브랜드 선언, 강의 등) ② 어떤 무드인가(자신감 있고 강렬 / 조용하고 문학적 / 따뜻하고 경쾌 / 어둡고 무게감 등)"를 물어본다. 사용자의 **취향**은 브리프만으로 추론되지 않으므로, 뻔해 보여도 반드시 묻는다.
2. **`docs/index.json`을 읽고 후보 3종을 고른다.** 각 템플릿의 `mood`·`tone`·`best_for`·`formality`를 사용자의 상황·무드와 대조한다. 3종은 **서로 충분히 달라서** 진짜 선택지가 되게 한다(에디토리얼 하나, 더 따뜻한 대안 하나, 브리프를 다르게 해석한 와일드카드 하나 식).
3. **후보별 표지 미리보기를 만든다.** 각 템플릿의 첫 슬라이드(표지)에 사용자의 실제 제목·부제·작성자·날짜를 넣어, 자기완결형 HTML 미리보기 3개를 만든다. 템플릿이 필요로 하는 형제 자산(styles.css, deck-stage.js 등)을 함께 둬 브라우저에서 바로 열리게 한다.
4. **3개 미리보기를 브라우저로 열고 경로를 보낸다.** 사용자가 실제 화면을 보고 고르게 한다. 고를 때까지 기다린다.
5. **고른 템플릿으로 전체 덱을 만든다.** 그 템플릿 폴더 전체를 복제해, 모든 슬라이드를 사용자 콘텐츠로 채운다. 이때 **보존할 것**(폰트·색 팔레트·레이아웃 그리드·데코)과 **교체할 것**(헤드라인·본문·숫자·이름·날짜)을 `AGENTS.md` §3에 따라 지킨다. 슬라이드가 모자라면 기존 레이아웃을 복제해 늘리고, 없는 레이아웃이 필요하면 `AGENTS.md` §5에 따라 그 템플릿의 디자인 시스템으로 새로 설계한다(다른 템플릿을 섞지 않는다).
6. **완성 덱을 열고 경로를 보고한다.**

지켜야 할 원칙(왜 중요한가):

- **미리보기 단계를 건너뛰지 않는다.** 사람은 톤을 글이 아니라 눈으로 판단한다. 표지 하나라도 실제로 보여 줘야 "이거다"가 나온다.
- **템플릿을 섞지 않는다.** 각 템플릿은 닫힌 시각 체계다. A 템플릿 표지에 B 템플릿 본문을 붙이면 아마추어처럼 보인다. 부족한 레이아웃은 고른 템플릿의 문법으로 새로 그린다.
- **디자인 시스템은 손대지 않는다.** 폰트·색·그리드·장식은 그 템플릿을 그 템플릿이게 하는 것이다. 바꾸는 것은 사용자의 콘텐츠뿐이다.

---

## 추출 모드

이미 있는 덱을 분석해 **그 덱과 똑같은 슬라이드를 다시 만들 수 있는 §1~§10 시스템 프롬프트**로 바꾼다.

"추측하지 말고 정확히"가 이 모드의 생명이다. 잘 만든 덱 한 벌에는 재사용 가능한 규칙(색·타이포·레이아웃·카피 톤·숫자 표기)이 통째로 들어 있고, 그 규칙을 정확한 값으로 뽑아 문서화하는 것이 목표다. 정확한 골격과 섹션별 채움 지침은 **`references/prompt-template.md`에 있으니 작업 시작 전 먼저 읽는다.**

### 입력 / 출력

| 항목 | 형식 |
|---|---|
| 입력 | PDF, 또는 PowerPoint(.pptx/.ppt) |
| 출력 | 마크다운 시스템 프롬프트 (§1~§10) |
| 언어 | 한국어 (코드·토큰·폰트명·HTML 예시는 원문) |
| 저장 | `~/Desktop/{원본파일명}-prompt.md` |
| 크기 | 대략 10~20KB (덱이 단순하면 더 짧아도 됨) |

### 워크플로우

**Step 1. 덱을 읽는다.** 전체를 훑어 어떤 슬라이드 유형이 있는지 파악하고, 대표 페이지 10~15장을 집중 분석한다. 페이지가 많아도 유형은 대개 5~10가지로 수렴한다.
- **PDF**: 네이티브로 읽어 텍스트·구조를 보고, 색·여백은 페이지를 이미지로 렌더해 확인한다(방법은 Step 2).
- **PPTX**: `.pptx`는 zip이다. 풀어서 `ppt/theme/theme1.xml`(색·폰트 테마), `ppt/slideMasters`, `ppt/slides/*.xml`을 직접 읽으면 PDF보다 정확한 값을 얻는다. pptx가 있으면 이 경로를 우선한다.

**Step 2. 디자인 시스템을 추출한다.** 정확한 색·폰트·여백 추출 방법은 **`references/analysis-framework.md`의 §2.0**을 따른다(페이지 렌더 → 픽셀 샘플링, `pdffonts`로 폰트 조회 등). 뽑을 것: 색상 토큰(전부, hex로), 타이포그래피(요소별 weight·size·spacing·line-height), 그리드·여백(단위 명시), 데코레이션(표지·섹션 라벨·카드·테이블·특수 장식). 확정 못 한 값은 지어내지 말고 `(근사)` 표시나 "정보 부족"으로 남긴다.

**Step 3. 실제 등장한 레이아웃을 카탈로그로 만든다.** **덱에 실제로 있는 레이아웃만** 유형별로 정리한다. 개수는 고정이 아니다. 3종만 쓰는 덱이면 3종만. 각 유형에 이름·용도·구조를 붙인다. 억지로 개수를 채우지 않는다.

**Step 4. 콘텐츠·한국어·숫자 규칙을 분석한다.** 카피가 어떻게 쓰였는지 규칙으로 뽑는다. 제목·부제 톤과 종결 표현, 강조 방식, 카드·테이블 텍스트 규칙, 한국어 표기 원칙(em dash·번역투·주술·종결 일관성), 숫자·단위·약어 표기. **상세 기준은 `references/content-rules.md`.**

**Step 5. §1~§10을 조립하고 저장한다.** `references/prompt-template.md`의 골격에 Step 2~4에서 관찰한 값을 채워 완전한 시스템 프롬프트를 만든다. §1~§10을 모두 포함하고, 각 섹션에 복제 가능한 구체 값을 최소 하나 넣는다. 관찰 못 한 섹션은 "정보 부족: {이유}"로 남긴다. 저장 후 사용자에게 짧게 보고한다(색 개수, 레이아웃 종류, 핵심 콘텐츠 규칙, 파일 경로). 보고 형식 예시는 `references/examples.md`.

### 산출물 골격 (§1~§10 한눈에)

| § | 섹션 | 핵심 |
|---|---|---|
| 1 | 역할과 기본 규격 | 역할 한 줄, 기본 출력 형식(HTML/PPTX), 비율 |
| 2 | 디자인 시스템 | 색 토큰, 타이포, 그리드, 데코 |
| 3 | 레이아웃 카탈로그 | 실제 등장한 레이아웃만 |
| 4 | 콘텐츠 작성 규칙 | 제목·강조·카드·테이블 카피 |
| 5 | 한국어 표기 원칙 | em dash·번역투·주술·종결 |
| 6 | 숫자·단위·약어 | 콤마·소수·단위 위치·시점 표기 |
| 7 | 데이터 시각화 | 차트·테이블·다이어그램 규격 |
| 8 | 접근성과 가독성 | 대비·최소 크기·정보 밀도 |
| 9 | 산출·기술 규격 | 폰트 로딩·네비·인쇄·자기완결성 |
| 10 | 품질 체크리스트 | 넘기기 전 자기 점검 목록 |

각 섹션에 무엇을 채우는지 상세는 `references/prompt-template.md`.

### 추출 모드 품질 기준 (이걸 넘겨야 완성)

1. **완결성**: §1~§10 모두 포함. 빈 섹션은 "정보 부족: {이유}"로 명시(빈칸 방치 금지).
2. **복제 가능성**: 각 섹션에 구체 값이 있다. "세련된 색"이 아니라 `#0B5A3C`. 추상 설명만 있는 섹션은 실패.
3. **정확성**: 색·폰트·여백은 Step 2 방법으로 확인한 값. 확정 못 하면 `(근사)` 표시. 정밀값을 지어내지 않는다.
4. **과적합 금지**: 레이아웃·색 개수를 특정 숫자로 강제하지 않는다. 이 덱에 있는 만큼만.
5. **한국어**: 모든 섹션 한국어(코드·토큰·폰트명·HTML 예시 제외).
6. **기본 출력 형식 명시**: §1에서 사용자 미지정 시 무엇을 만들지(HTML 또는 PPTX) 못 박는다.

---

## 관련 자료

**선택·제작 모드**
- `AGENTS.md` (저장소 루트) — 선택→미리보기→제작 6단계 전체 워크플로우, 톤 매칭 기준(§4), 템플릿 확장 규칙(§5), 함정(§6). 이 모드에 들어가면 먼저 읽는다.
- `docs/index.json` — 35종 템플릿의 무드·톤·용도·격식 메타데이터 카탈로그.
- `docs/templates/<slug>/` — 각 템플릿의 실제 파일(template.html 등).

**추출 모드**
- `references/prompt-template.md` — §1~§10 골격과 섹션별 채움 지침 (작업 전 필독).
- `references/analysis-framework.md` — 추출 방법론(§2.0)과 색·타이포·그리드·데코 상세 기준.
- `references/content-rules.md` — 콘텐츠·한국어·숫자 표기 상세 규칙.
- `references/examples.md` — 워크드 예제와 보고 형식.
